# wad command reference

Generated by `python tools/gen_docs.py` from the command declarations - the same
ones that build the command line and the MCP tools. Every command also takes
`--json`. Targets are a ref from the last snapshot (`e12`) or a selector
(`role=Button name=Save`, see [SELECTORS.md](SELECTORS.md)).

| Group | Commands |
|---|---|
| observe | [`watch`](#watch), [`windows`](#windows), [`snapshot`](#snapshot), [`find`](#find), [`get`](#get), [`wait`](#wait) |
| act | [`click`](#click), [`type`](#type), [`clear`](#clear), [`check`](#check), [`uncheck`](#uncheck), [`expand`](#expand), [`collapse`](#collapse), [`select`](#select) |
| mouse | [`right-click`](#right-click), [`double-click`](#double-click), [`hover`](#hover), [`drag`](#drag), [`scroll`](#scroll), [`scroll-to`](#scroll-to) |
| keyboard | [`press`](#press), [`input-lang`](#input-lang), [`clipboard`](#clipboard) |
| windows | [`launch`](#launch), [`focus`](#focus), [`window`](#window), [`close`](#close) |
| vision | [`screenshot`](#screenshot), [`click-xy`](#click-xy), [`ocr`](#ocr), [`click-text`](#click-text), [`detect`](#detect), [`click-mark`](#click-mark) |
| browser | [`browser-launch`](#browser-launch), [`browser-tabs`](#browser-tabs), [`browser-open`](#browser-open), [`browser-snapshot`](#browser-snapshot), [`browser-click`](#browser-click), [`browser-type`](#browser-type), [`browser-text`](#browser-text), [`browser-wait`](#browser-wait), [`browser-eval`](#browser-eval), [`browser-screenshot`](#browser-screenshot), [`browser-close`](#browser-close) |
| office | [`excel-info`](#excel-info), [`excel-read`](#excel-read), [`excel-write`](#excel-write), [`excel-run`](#excel-run), [`word-read`](#word-read), [`word-write`](#word-write), [`outlook-list`](#outlook-list), [`outlook-read`](#outlook-read), [`outlook-draft`](#outlook-draft), [`outlook-send`](#outlook-send), [`ppt-read`](#ppt-read), [`ppt-add-slide`](#ppt-add-slide), [`ppt-replace`](#ppt-replace), [`ppt-save`](#ppt-save) |
| system | [`shell`](#shell), [`file-read`](#file-read), [`file-write`](#file-write), [`file-list`](#file-list), [`process-list`](#process-list), [`process-kill`](#process-kill) |
| workflow | [`record`](#record), [`record-stop`](#record-stop), [`report`](#report), [`batch`](#batch), [`trace`](#trace), [`doctor`](#doctor), [`guide`](#guide), [`mcp`](#mcp) |

## Observe - see what is on screen

### watch

Listen for a while and report what the desktop did: windows and menus opening/closing, focus moves (UIA events).

```
wad watch [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--seconds` | number | `10.0` | how long to listen |
| `--window` `-w` | text |  | only events from this window's app |
| `--no-focus` | flag |  | leave out focus changes |
| `--until` | text |  | stop early when an event's name contains this text |

_read-only_

### windows

List top-level windows (title, handle, process).

```
wad windows
```

_read-only_

### snapshot

Accessibility tree of a window, with refs for every element.

```
wad snapshot [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |
| `--interactive` `-i` | flag |  | only interactive elements and the containers leading to them |
| `--depth` | integer |  | skeleton: stop at this depth (then drill in with --root) |
| `--root` | text |  | drill down: snapshot only below this ref |
| `--popup` | flag |  | snapshot the app's open menu / dialog instead of its window |
| `--limit` | integer | `4000` | stop after this many elements |

_read-only_

### find

Search the last snapshot by role / name / id (prints refs and selectors).

```
wad find [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--role` | text |  |  |
| `--name` | text |  | substring, case-insensitive |
| `--aid` | text |  | AutomationId |
| `--interactive` `-i` | flag |  | only interactive elements |

_read-only_

### get

Read an element's live state: value, text, toggle, expanded, selected....

```
wad get TARGET [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | a ref from the last snapshot (e12) or a selector (role=Button name=Save) |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |

_read-only_

### wait

Wait until text / an element / a window appears (or is gone).

```
wad wait [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--window` `-w` | text |  | window title; with nothing else, wait for the window |
| `--hwnd` | integer |  |  |
| `--name` | text |  | text to wait for in any element name or field value |
| `--target` | text |  | a selector to wait for (role=Button name=OK) |
| `--gone` | flag |  | wait until it disappears instead |
| `--timeout` | number | `10.0` |  |

_read-only_

## Act - through accessibility (no mouse, verified)

### click

Activate an element through accessibility (no mouse); --headed for a real, guarded mouse click.

```
wad click TARGET [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | a ref from the last snapshot (e12) or a selector (role=Button name=Save) |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |
| `--headed` | flag |  | real mouse click at the element's center |
| `--expect` | text |  | fail unless this text appears in the app afterwards |
| `--expect-gone` | text |  | fail unless this text disappears from the app afterwards |
| `--timeout` | number | `5.0` | seconds to wait for --expect/--expect-gone |
| `--no-verify` | flag |  | skip the after-action check (faster, blind) |

### type

Put text into a field: value pattern first, verified, and automatically retyped as real Unicode keystrokes if the app ignored it.

```
wad type TARGET TEXT [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | a ref from the last snapshot (e12) or a selector (role=Button name=Save) |
| TEXT | text |  | the text (any language) |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |
| `--append` | flag |  | add to the end instead of replacing |
| `--keys` | flag |  | skip the value pattern and type keystrokes directly |
| `--submit` | flag |  | press Enter afterwards |
| `--expect` | text |  | fail unless this text appears in the app afterwards |
| `--expect-gone` | text |  | fail unless this text disappears from the app afterwards |
| `--timeout` | number | `5.0` | seconds to wait for --expect/--expect-gone |
| `--no-verify` | flag |  | skip the after-action check (faster, blind) |

### clear

Empty a text field (verified).

```
wad clear TARGET [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | a ref from the last snapshot (e12) or a selector (role=Button name=Save) |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |

### check

Switch a checkbox / toggle ON (no-op if already on; verified).

```
wad check TARGET [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | a ref from the last snapshot (e12) or a selector (role=Button name=Save) |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |

### uncheck

Switch a checkbox / toggle OFF (verified).

```
wad uncheck TARGET [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | a ref from the last snapshot (e12) or a selector (role=Button name=Save) |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |

### expand

Open a combo box, tree item, menu or ribbon drop-down.

```
wad expand TARGET [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | a ref from the last snapshot (e12) or a selector (role=Button name=Save) |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |

### collapse

Close a combo box, tree item or drop-down.

```
wad collapse TARGET [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | a ref from the last snapshot (e12) or a selector (role=Button name=Save) |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |

### select

Pick an option by its text in a combo box, list, tab strip or tree (verified).

```
wad select TARGET OPTION [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | the combo box / list (ref or selector) |
| OPTION | text |  | the option's visible text (exact, else substring) |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |

## Mouse - real pointer, guarded

### right-click

Real right click on an element (opens context menus; then `snapshot --popup`).

```
wad right-click TARGET [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | a ref from the last snapshot (e12) or a selector (role=Button name=Save) |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |
| `--expect` | text |  | fail unless this text appears in the app afterwards |
| `--expect-gone` | text |  | fail unless this text disappears from the app afterwards |
| `--timeout` | number | `5.0` | seconds to wait for --expect/--expect-gone |
| `--no-verify` | flag |  | skip the after-action check (faster, blind) |

### double-click

Real double click on an element.

```
wad double-click TARGET [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | a ref from the last snapshot (e12) or a selector (role=Button name=Save) |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |
| `--expect` | text |  | fail unless this text appears in the app afterwards |
| `--expect-gone` | text |  | fail unless this text disappears from the app afterwards |
| `--timeout` | number | `5.0` | seconds to wait for --expect/--expect-gone |
| `--no-verify` | flag |  | skip the after-action check (faster, blind) |

### hover

Move the real mouse pointer over an element (tooltips, hover menus).

```
wad hover TARGET [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | a ref from the last snapshot (e12) or a selector (role=Button name=Save) |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |

### drag

Drag with the real mouse from one element to another (or between points).

```
wad drag [SOURCE] [DEST] [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| SOURCE | text |  | ref or selector to drag from |
| DEST | text |  | ref or selector to drop on |
| `--from-xy` | text |  | X,Y screen pixels instead of a source element |
| `--to-xy` | text |  | X,Y screen pixels instead of a destination element |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |

### scroll

Scroll a list/document/page (accessibility scroll; --headed uses the mouse wheel).

```
wad scroll TARGET [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | the scrollable element or anything inside it |
| `--direction` | down \| up \| left \| right | `down` |  |
| `--amount` | integer | `3` | steps (small increments or wheel notches) |
| `--page` | flag |  | scroll by pages instead of small steps |
| `--headed` | flag |  | use the real mouse wheel |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |

### scroll-to

Scroll an element into view.

```
wad scroll-to TARGET [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | a ref from the last snapshot (e12) or a selector (role=Button name=Save) |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |

## Keyboard and clipboard

### press

Key chord or sequence to the app, guarded: 'ctrl+s', 'alt+f4', 'alt h o r' (a sequence, e.g. Office key tips).

```
wad press COMBO [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| COMBO | text |  | chords joined by +, sequence separated by spaces |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |
| `--interval` | number | `0.4` | pause between chords of a sequence |

### input-lang

Show or switch the keyboard layout of ONE window (en, ar, or a KLID); Office key tips need en.

```
wad input-lang [LANG] [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| LANG | text |  | en | ar | fr | de | 00000409 ... |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |

### clipboard

Read, set or clear the clipboard text.

```
wad clipboard ACTION [TEXT]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| ACTION | get \| set \| clear |  |  |
| TEXT | text |  |  |

## Windows and apps

### launch

Start an app (known name, exe, path, Start-menu or Store app name) and wait for its NEW window.

```
wad launch APP [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| APP | text |  | calculator | notepad | excel.exe | C:\path\app.exe | a Start-menu name |
| `--title` | text |  | window title to wait for (default: the app name) |
| `--timeout` | number | `20.0` | seconds to wait for the window |

### focus

Give keyboard focus to an element, or bring a window to the front.

```
wad focus [TARGET] [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | ref or selector (omit for window) |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |

### window

Minimize / maximize / restore / move / resize / front a window.

```
wad window ACTION [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| ACTION | minimize \| maximize \| restore \| move \| resize \| front |  |  |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |
| `--x` | integer |  |  |
| `--y` | integer |  |  |
| `--width` | integer |  |  |
| `--height` | integer |  |  |

### close

Close a window politely (its own close action; the app may ask to save).

```
wad close [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |

## Vision - pixels and OCR, for apps without a tree

### screenshot

Capture a window (or the screen) to PNG; coordinates in it can be used directly by click-xy.

```
wad screenshot [OUT] [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| OUT | text |  | PNG path (default: state dir) |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |
| `--screen` | flag |  | the whole screen instead of a window |
| `--region` | text |  | X,Y,WIDTH,HEIGHT inside the window, to zoom in |
| `--marks` | flag |  | draw the last snapshot's refs on the picture (needs Pillow) |
| `--max-width` | integer | `1600` | downscale wider images (0 = never) |

_read-only, MCP result includes the image_

### click-xy

Real mouse click at a point of the last screenshot (or --screen pixels).

```
wad click-xy X Y [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| X | number |  |  |
| Y | number |  |  |
| `--button` | left \| right \| middle | `left` |  |
| `--double` | flag |  | double click |
| `--screen` | flag |  | x,y are absolute screen pixels |
| `--expect` | text |  | fail unless this text appears afterwards |
| `--timeout` | number | `5.0` |  |

### ocr

Read the text of a window/screen from pixels (Windows OCR), with positions usable by click-xy.

```
wad ocr [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |
| `--screen` | flag |  |  |
| `--region` | text |  | X,Y,WIDTH,HEIGHT in the window |
| `--lang` | text |  | OCR language tag, e.g. en, ar (default: the user's languages) |
| `--find` | text |  | only report where this text is |

_read-only_

### click-text

Find text on screen by OCR and click it (for apps with no accessibility tree).

```
wad click-text TEXT [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TEXT | text |  |  |
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |
| `--nth` | integer | `1` | which match, from the top-left |
| `--button` | left \| right | `left` |  |
| `--double` | flag |  |  |
| `--lang` | text |  | OCR language tag |
| `--expect` | text |  | fail unless this text appears afterwards |
| `--timeout` | number | `5.0` |  |

### detect

Find buttons, icons and text in the PIXELS with a vision model (OmniParser server) - for apps with no accessibility tree; refs v1.. work with click-mark.

```
wad detect [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--window` `-w` | text |  | window title (exact, else a unique substring) |
| `--hwnd` | integer |  | window handle (from `windows`) |
| `--screen` | flag |  |  |
| `--region` | text |  | X,Y,WIDTH,HEIGHT in the window |
| `--interactive` `-i` | flag |  | only elements the model thinks are clickable |
| `--url` | text |  | OmniParser server (default WAD_OMNIPARSER_URL or 127.0.0.1:8000) |
| `--marks` | flag |  | also save a picture with the v-refs drawn on it |

_read-only, MCP result includes the image_

### click-mark

Real mouse click on an element found by `detect` (v12), guarded.

```
wad click-mark REF [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| REF | text |  | v-ref from the last detect |
| `--button` | left \| right | `left` |  |
| `--double` | flag |  |  |
| `--expect` | text |  | fail unless this text appears afterwards |
| `--timeout` | number | `5.0` |  |

## Browser - Edge / Chrome from the inside (DevTools protocol)

### browser-launch

Start Edge or Chrome with its own profile and a local-only DevTools port, for the browser-* commands.

```
wad browser-launch [URL] [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| URL | text |  | page to open |
| `--browser` | edge \| chrome | `edge` |  |
| `--port` | integer | `9222` |  |
| `--headless` | flag |  | no visible window |
| `--exe` | text |  | path to another Chromium-based browser (Brave, Chromium, ...) |

### browser-tabs

List the wad browser's tabs; --use N makes tab N the current one.

```
wad browser-tabs [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--use` | integer |  | 1-based tab number to make current |

_read-only_

### browser-open

Go to a URL in the current tab (or --new-tab) and wait for it to load.

```
wad browser-open URL [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| URL | text |  |  |
| `--new-tab` | flag |  |  |
| `--expect` | text |  | fail unless this text appears on the page |
| `--timeout` | number | `15.0` |  |

### browser-snapshot

The page's clickable and fillable elements, with refs (b12).

```
wad browser-snapshot [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--limit` | integer | `300` |  |

_read-only_

### browser-click

Click an element of the page with a real (trusted) mouse event.

```
wad browser-click TARGET [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | b12 | text=Sign in | a CSS selector |
| `--nth` | integer |  | which match, when several do |
| `--expect` | text |  | fail unless this text appears on the page afterwards |
| `--timeout` | number | `5.0` |  |

### browser-type

Type into a page field like a person (works with React/Angular forms); read back.

```
wad browser-type TARGET TEXT [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | b12 | text=Email | a CSS selector |
| TEXT | text |  |  |
| `--nth` | integer |  | which match, when several do |
| `--append` | flag |  | add to what is there |
| `--submit` | flag |  | press Enter afterwards |

### browser-text

The visible text of the page (or of one element).

```
wad browser-text [TARGET] [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TARGET | text |  | b12 | text=... | CSS (default: page) |
| `--max-chars` | integer | `20000` |  |

_read-only_

### browser-wait

Wait until text is on the page (or --gone).

```
wad browser-wait TEXT [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TEXT | text |  |  |
| `--gone` | flag |  |  |
| `--timeout` | number | `10.0` |  |

_read-only_

### browser-eval

Run JavaScript in the page and return its (JSON) result.

```
wad browser-eval SCRIPT
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| SCRIPT | text |  | an expression; wrap statements in (() => {...})() |

### browser-screenshot

Capture the page as PNG (the viewport).

```
wad browser-screenshot [OUT]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| OUT | text |  |  |

_read-only, MCP result includes the image_

### browser-close

Close the wad browser (and its DevTools port).

```
wad browser-close
```

## Office - Excel and Word through their object model

### excel-info

Open workbooks, their sheets, used ranges and the selection.

```
wad excel-info [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--start` | flag |  | start Excel if it is not running |

_read-only_

### excel-read

Read a range's values (numbers stay numbers, errors as #DIV/0! ...).

```
wad excel-read [RANGE] [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| RANGE | text |  | A1:D20, a name, or omit for the used range |
| `--book` | text |  | workbook name (default: the active one) |
| `--sheet` | text |  | sheet name (default: the active one) |
| `--start` | flag |  | start Excel if it is not running |
| `--formulas` | flag |  | return formulas instead of values |
| `--text` | flag |  | return what Excel displays (formatted), cell by cell |

_read-only_

### excel-write

Write values or formulas starting at a cell; read back and checked.

```
wad excel-write RANGE [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| RANGE | text |  | top-left cell, e.g. B2 |
| `--values` | text |  | JSON: 5, [1,2,3] (a row) or [[1,2],[3,4]]; strings starting with = are formulas |
| `--tsv` | text |  | tab-separated rows instead of JSON |
| `--book` | text |  | workbook name (default: the active one) |
| `--sheet` | text |  | sheet name (default: the active one) |
| `--start` | flag |  | start Excel if it is not running |

### excel-run

Run an Excel command on the workbook: save, save-as, calculate, autofit, select.

```
wad excel-run ACTION [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| ACTION | save \| save-as \| calculate \| autofit \| select |  |  |
| `--target` | text |  | save-as: file path; autofit/select: a range |
| `--book` | text |  | workbook name (default: the active one) |
| `--sheet` | text |  | sheet name (default: the active one) |
| `--start` | flag |  | start Excel if it is not running |

### word-read

The text of an open Word document, paragraph by paragraph.

```
wad word-read [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--doc` | text |  | document name (default: the active one) |
| `--start` | flag |  | start Word if it is not running |

_read-only_

### word-write

Add text to an open Word document (end, start, or replace text); read back and checked.

```
wad word-write TEXT [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| TEXT | text |  |  |
| `--at` | end \| start \| replace | `end` |  |
| `--find` | text |  | with --at replace: the text to replace (all occurrences) |
| `--doc` | text |  | document name (default: the active one) |
| `--start` | flag |  | start Word (with a new document) if it is not running |

### outlook-list

Recent mail in a folder: sender, subject, time, unread (newest first).

```
wad outlook-list [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--folder` | text | `inbox` | inbox | sent | drafts | deleted | Inbox/Sub |
| `--limit` | integer | `20` |  |
| `--unread` | flag |  | only unread |
| `--search` | text |  | only subjects or senders containing this |
| `--start` | flag |  | start Outlook if it is not running |

_read-only_

### outlook-read

One message: headers, text, attachment names.

```
wad outlook-read WHICH [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| WHICH | text |  | its number in the last outlook-list, or its id |
| `--max-chars` | integer | `20000` |  |
| `--start` | flag |  |  |

_read-only_

### outlook-draft

Write a mail and save it in Drafts - never sends; read back.

```
wad outlook-draft [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--to` | text |  | addresses, separated by ; (required) |
| `--subject` | text |  |  (required) |
| `--body` | text | `` |  |
| `--cc` | text |  |  |
| `--attach` | text |  | file paths, separated by ; |
| `--show` | flag |  | also open it on screen for the person to review |
| `--start` | flag |  |  |

### outlook-send

SEND a saved draft - only when the policy allows sending.

```
wad outlook-send WHICH [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| WHICH | text |  | the draft's id (from outlook-draft) |
| `--start` | flag |  |  |

### ppt-read

The text of every slide (or one), with slide titles.

```
wad ppt-read [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--slide` | integer |  | only this slide number |
| `--pres` | text |  | presentation name (default: the active one) |
| `--start` | flag |  | start PowerPoint if it is not running |

_read-only_

### ppt-add-slide

Add a slide with a title and body text; read back.

```
wad ppt-add-slide [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--title` | text |  |  (required) |
| `--body` | text | `` | lines separated by \n |
| `--layout` | title-content \| title-only \| blank \| title | `title-content` |  |
| `--at` | integer |  | position (default: the end) |
| `--pres` | text |  | presentation name (default: the active one) |
| `--start` | flag |  | start PowerPoint if it is not running |

### ppt-replace

Replace text on every slide (shapes and tables); counts and checks.

```
wad ppt-replace [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--find` | text |  |  (required) |
| `--replace` | text |  | the new text (required) |
| `--pres` | text |  | presentation name (default: the active one) |
| `--start` | flag |  | start PowerPoint if it is not running |

### ppt-save

Save the presentation (or --to a new file: .pptx, .pdf).

```
wad ppt-save [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--to` | text |  | save as this path instead; .pdf exports a PDF |
| `--pres` | text |  | presentation name (default: the active one) |
| `--start` | flag |  | start PowerPoint if it is not running |

## System - shell, files, processes (off by default)

### shell

Run a PowerShell command and return its output (policy-gated; dangerous commands always refused).

```
wad shell COMMAND [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| COMMAND | text |  |  |
| `--timeout` | number | `60.0` |  |
| `--cwd` | text |  | working directory |

### file-read

Read a text file (policy-gated).

```
wad file-read PATH [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| PATH | text |  |  |
| `--max-chars` | integer | `100000` |  |

_read-only_

### file-write

Write a text file inside the policy's write_roots (read back).

```
wad file-write PATH TEXT [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| PATH | text |  |  |
| TEXT | text |  |  |
| `--append` | flag |  |  |

### file-list

List a folder (policy-gated).

```
wad file-list PATH [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| PATH | text |  |  |
| `--pattern` | text |  | only names containing this |

_read-only_

### process-list

Running processes (policy-gated).

```
wad process-list [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--name` | text |  | filter |

_read-only_

### process-kill

End a process by pid or exe name (needs allow_kill; system processes always refused).

```
wad process-kill WHICH
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| WHICH | text |  | pid or exe name |

## Workflow - batch, replay, setup

### record

Watch a PERSON do a task and save it as a replayable batch file (stop with Ctrl+Shift+F12, `record-stop`, or --seconds).

```
wad record [OUT] [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| OUT | text |  | the batch file to write |
| `--window` `-w` | text |  | only record in windows whose title contains this |
| `--seconds` | number |  | stop by itself after this long |
| `--ignore-injected` | flag |  | skip synthetic input (other tools, remote control) - only real hands |

_command line only_

### record-stop

Stop a running `record` (from another terminal or agent).

```
wad record-stop
```

### report

The visual step report (OFF by default): on | off | status | build | clear.

```
wad report ACTION [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| ACTION | on \| off \| status \| build \| clear |  |  |
| `--out` | text |  | build: the HTML file to write (default: in the report folder) |

### batch

Run a JSON list of steps in one go (a recorded or hand-written workflow).

```
wad batch FILE [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| FILE | text |  | JSON: [{"cmd": "click", "target": "role=Button name=OK"}, {"sleep": 0.5}, ...] |
| `--keep-going` | flag |  | continue after a failed step |
| `--no-heal` | flag |  | fail on a changed element instead of healing the step |
| `--save-healed` | flag |  | write healed selectors back into the batch file |

### trace

The action log: last steps, or --export a replayable batch file.

```
wad trace [options]
```

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `--last` | integer | `20` | how many entries |
| `--export` | text |  | write the recorded steps of the last --last entries to this batch file |

_read-only_

### doctor

Check this PC: Windows, Python, packages, elevation, DPI, keyboard, policy.

```
wad doctor
```

_read-only_

### guide

Print the agent playbook (how to drive Windows apps well with wad).

```
wad guide
```

_read-only_

### mcp

Serve every command as MCP tools over stdio (for Claude, Cursor, ...).

```
wad mcp
```

_command line only_
